# Consumed by the official postgres image's own entrypoint (different
# variable names than ignis's DB_* in app.env; same values, kept in sync
# manually).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ignis
